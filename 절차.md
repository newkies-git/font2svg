# 한자 획순 애니메이션 제작 및 운영 절차 (Procedures)

본 프로젝트의 결과물을 유지보수하거나, 새로운 한자를 대량으로 추가하기 위한 표준 절차입니다.

---

## 1. 환경 구축 (Setup)

### 1.1 저장소 준비
- 프로젝트 폴더 구조를 유지합니다.
- 한자 JSON 폴더명(예시): **`opnemind_data`**(구 `data1`), **`biaukai_data`**(구 `data2`), **`hanzi_data`**(구 `data3`). `animator.html` 사이드바에서 **`opnemind_data` / `biaukai_data` / `hanzi_data`** 중 하나를 선택합니다.

### 1.2 의존성 설치 및 실행 환경
- Python 3.9 이상 버전이 권장됩니다.
- 가상환경 활성화 (권장):
  ```bash
  source venv/bin/activate
  ```
- 필수 라이브러리 설치:
  ```bash
  pip install fonttools
  ```

---

## 2. 한자 데이터 생성 및 업데이트 (Workflow)

### 1단계: 추출 대상 정의
- `input.json` 파일을 열어 추출하고자 하는 한자의 리스트를 업데이트합니다.
- 형식: `{"id": "고유ID", "한자": "한자글자", "음": "소리"}`

### 2단계: 폰트 윤곽선 일괄 추출
- `font_extractor.py`를 실행하여 폰트 파일에서 벡터 데이터를 추출합니다. 대상 한자는 **`--input`(기본 `input.json`)** 의 `한자` 필드에서 읽습니다.
- **예시** (`--outdir`에 한자별 JSON 생성):
  ```bash
  ./venv/bin/python font_extractor.py --input input.json --font NotoSerifKR-Regular.ttf --outdir font_ext_data
  ```
- `--outdir`를 생략하면 기본 **`font_ext_data`**에 씁니다. `animator.html`이 읽는 `opnemind_data` 등과 다를 수 있으므로, 필요하면 복사·동기화하거나 `--outdir`로 애니메이터용 폴더를 직접 지정합니다.
- **TTC 폰트**(예: BiauKai): `--font-family` 또는 `--font-index`, 면 목록은 `--list-faces`.
- 기존 `{한자}.json`이 있으면 **`radical`(글자 수준)** 등 일부 필드는 유지하고 `font_outline` / `stroke_outlines` 를 갱신합니다.

### 3단계: 로컬 개발 서버 실행
- 보안 정책(CORS)으로 인해 브라우저에서 직접 열면 데이터 로딩이 불가능합니다. 반드시 로컬 서버를 통해 접속하십시오.
  ```bash
  python3 -m http.server 8000
  ```
  - 잘못된 예: `python3 -m localhost:8000` (`localhost:8000`은 모듈 이름이 아닙니다.)
- 접속 주소: `http://localhost:8000/animator.html`

### 4단계 (선택): `hanzi_fetch`를 hanzi-writer-data로 채우기
- 스크립트: 저장소 루트의 [`fetch_hanzi_writer_to_data2.py`](fetch_hanzi_writer_to_data2.py) 를 실행합니다.
- `input.json`에 등록된 **한자**마다 CDN  
  `https://cdn.jsdelivr.net/npm/hanzi-writer-data@2.0/{한자}.json`  
  을 조회하고, 결과를 애니메이터용 한자 JSON(예: [`opnemind_data/佳.json`](opnemind_data/佳.json))과 동일한 JSON 스키마(`char`, `radical`, `font_outline`, `stroke_outlines`)로 **`hanzi_fetch/`**에 저장합니다.
- **`hanzi_fetch/{한자}.json`이 이미 있으면 덮어쓰지 않고 건너뜁니다.**
- `--outdir`를 생략하면 기본 **`hanzi_fetch`**입니다. 사이드바의 `biaukai_data` 등 다른 폴더에 넣으려면 `--outdir`로 지정하거나 이후에 파일을 옮깁니다.
- 패키지에 없는 글자는 404로 파일이 생기지 않습니다(로그에 `MISS` 출력).
```bash
./venv/bin/python -u fetch_hanzi_writer_to_data2.py
# 옵션: --delay 0.02 (요청 간격 초), --dry-run
```

---

## 3. 획순(Skeleton) 정교화 작업

추출된 폰트 윤곽선 위에 애니메이션을 위한 뼈대를 입히는 과정입니다. (과거 문서 기준 `skeletons` 필드가 있었다면, 현재 애니메이터는 **`stroke_outlines` / `font_outline`** 중심으로 동작합니다.)

1. `animator.html`에서 대상 한자를 검색하여 로드합니다(사이드바에서 **`opnemind_data` / `biaukai_data` / `hanzi_data`** 중 폴더 선택).
2. 편집할 경로에 맞는 `opnemind_data/{한자}.json`, `biaukai_data/{한자}.json`, 또는 `hanzi_data/{한자}.json` 파일을 열어 필요한 배열을 편집합니다.
3. SVG `M`, `L`, `Q`, `C` 명령어를 사용하여 글자의 중심선을 정의합니다.
4. 브라우저에서 **Reset**과 **Animate** 버튼을 눌러 실시간으로 검증합니다.
5. **Horizontal/Scale/Offset** 슬라이더를 사용하여 시각적으로 가장 안정적인 위치를 찾습니다. (추천 오프셋: 870)

---

## 4. 유지보수 주의사항

- **파일 이름**: `opnemind_data/`, `biaukai_data/`, `hanzi_data/` 등 한자 JSON 폴더 안의 파일명은 반드시 **한자 글자 그 자체**여야 합니다 (예: `懇.json`).
- **인코딩**: 모든 데이터 파일은 UTF-8 인코딩을 유지해야 한글/한자가 깨지지 않습니다.
- **백업**: `skeletons` 데이터는 수동 작업의 산물이므로, 폰트 추출 작업을 수행하기 전에 항상 Git 등으로 백업을 유지하는 것이 좋습니다.

---
*Created by Antigravity*
